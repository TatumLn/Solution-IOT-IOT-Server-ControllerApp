<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="IOT_Controller.DesignView.TroisCercleIndicateur">

    <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

        <StackLayout Grid.Row="0">

            <CollectionView ItemsSource="{Binding TxtTroisIndicateur}" 
                            HorizontalOptions="Center">
                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Horizontal" 
                                     HorizontalItemSpacing="15"/>
                </CollectionView.ItemsLayout>
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Border HeightRequest="70"
                                    WidthRequest="70"
                                    StrokeShape="RoundRectangle 40"
                                    Stroke="#D9D9D9"
                                    StrokeThickness="7"
                                    Margin="5">
                                <Frame x:Name="frame"
                                       CornerRadius="40" 
                                       BackgroundColor="#19CBC0" 
                                       Padding="10" 
                                       HeightRequest="70" 
                                       WidthRequest="70"
                                       BindingContext="{Binding}">
                                    <Label Text="{Binding TxtTroisIndicateur}"
                                           TextColor="White"
                                           FontFamily="Inter"
                                           FontSize="15"
                                           HorizontalOptions="Center"
                                           VerticalOptions="Center">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" 
                                                         Binding="{Binding IsTriggered}">
                                                <Setter Property="TextColor" Value="OrangeRed"/>
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                </Frame>
                            </Border>
                        </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
    
        </StackLayout>

        <StackLayout Grid.Row="1">
            <CollectionView ItemsSource="{Binding TxtTroisIndicateur}"
                            HorizontalOptions="Center"
                            Margin="0,10,15,0">
            <CollectionView.ItemsLayout>
                <GridItemsLayout Orientation="Horizontal" 
                                 HorizontalItemSpacing="25"/>
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Label Text="{Binding}"
                           TextColor="White"/>
                </DataTemplate>
            </CollectionView.ItemTemplate>
    </CollectionView>
            </StackLayout>
        
        </Grid>

</ContentView>
    